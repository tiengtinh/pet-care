- symlink .claude CLAUDE.md (or vice verse) so these serves as single source of truth and works for codex as well

- ```
"dev:test:backend": "ENABLE_TESTING_API=true DATABASE_URL=postgresql://user:password@127.0.0.1:5432/pet_crm?schema=e2e PORT=3000 npm run dev",
```
not safe?

